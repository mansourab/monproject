{% extends 'base.html.twig' %}

{% block body %}
    <h1>Create new Item</h1>
    {{ form_start(form) }}
        {{ form_widget(form) }}
    <button> Create new one </button>
    {{ form_end(form) }}

{% endblock %}