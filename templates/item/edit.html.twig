{% extends "base.html.twig" %}

{% block body %}

    <h2>Editer l'item</h2>

    {{ form_start(form) }}
    {{ form_widget(form) }}

    <button>Update</button>
    
    {{ form_end(form) }}

{% endblock %}
